# Senior QA Agent
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: senior-qa
  namespace: agentic-qa
  labels:
    app: senior-qa
    component: qa-agent
    role: senior
spec:
  replicas: 1
  selector:
    matchLabels:
      app: senior-qa
  template:
    metadata:
      labels:
        app: senior-qa
        component: qa-agent
        role: senior
    spec:
      securityContext:
        fsGroup: 1000
        runAsNonRoot: true
      containers:
      - name: senior-qa
        image: ghcr.io/maccracken/agnostic-senior-qa:latest
        imagePullPolicy: IfNotPresent
        securityContext:
          allowPrivilegeEscalation: false
          runAsUser: 1000
          readOnlyRootFilesystem: true
          capabilities:
            drop:
            - ALL
        env:
        - name: AGENT_ROLE
          value: "senior"
        - name: OPENAI_API_KEY
          valueFrom:
            secretKeyRef:
              name: qa-secrets
              key: openai-api-key
        envFrom:
        - configMapRef:
            name: qa-config
        ports:
        - containerPort: 8002
          name: health
        resources:
          requests:
            memory: "1.5Gi"
            cpu: "1"
          limits:
            memory: "3Gi"
            cpu: "2"
        volumeMounts:
        - name: tmp
          mountPath: /tmp
        - name: logs
          mountPath: /app/logs
        livenessProbe:
          httpGet:
            path: /health
            port: 8002
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 8002
          initialDelaySeconds: 10
          periodSeconds: 5
        startupProbe:
          httpGet:
            path: /health
            port: 8002
          initialDelaySeconds: 10
          periodSeconds: 10
          failureThreshold: 30
          timeoutSeconds: 3
      volumes:
      - name: tmp
        emptyDir: {}
      - name: logs
        emptyDir: {}

# Junior QA Agent
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: junior-qa
  namespace: agentic-qa
  labels:
    app: junior-qa
    component: qa-agent
    role: junior
spec:
  replicas: 1
  selector:
    matchLabels:
      app: junior-qa
  template:
    metadata:
      labels:
        app: junior-qa
        component: qa-agent
        role: junior
    spec:
      securityContext:
        fsGroup: 1000
        runAsNonRoot: true
      containers:
      - name: junior-qa
        image: ghcr.io/maccracken/agnostic-junior-qa:latest
        imagePullPolicy: IfNotPresent
        securityContext:
          allowPrivilegeEscalation: false
          runAsUser: 1000
          readOnlyRootFilesystem: true
          capabilities:
            drop:
            - ALL
        env:
        - name: AGENT_ROLE
          value: "junior"
        - name: OPENAI_API_KEY
          valueFrom:
            secretKeyRef:
              name: qa-secrets
              key: openai-api-key
        envFrom:
        - configMapRef:
            name: qa-config
        ports:
        - containerPort: 8003
          name: health
        resources:
          requests:
            memory: "1Gi"
            cpu: "750m"
          limits:
            memory: "2Gi"
            cpu: "1500m"
        volumeMounts:
        - name: tmp
          mountPath: /tmp
        - name: logs
          mountPath: /app/logs
        livenessProbe:
          httpGet:
            path: /health
            port: 8003
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 8003
          initialDelaySeconds: 10
          periodSeconds: 5
        startupProbe:
          httpGet:
            path: /health
            port: 8003
          initialDelaySeconds: 10
          periodSeconds: 10
          failureThreshold: 30
          timeoutSeconds: 3
      volumes:
      - name: tmp
        emptyDir: {}
      - name: logs
        emptyDir: {}

# QA Analyst Agent
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: qa-analyst
  namespace: agentic-qa
  labels:
    app: qa-analyst
    component: qa-agent
    role: analyst
spec:
  replicas: 1
  selector:
    matchLabels:
      app: qa-analyst
  template:
    metadata:
      labels:
        app: qa-analyst
        component: qa-agent
        role: analyst
    spec:
      securityContext:
        fsGroup: 1000
        runAsNonRoot: true
      containers:
      - name: qa-analyst
        image: ghcr.io/maccracken/agnostic-qa-analyst:latest
        imagePullPolicy: IfNotPresent
        securityContext:
          allowPrivilegeEscalation: false
          runAsUser: 1000
          readOnlyRootFilesystem: true
          capabilities:
            drop:
            - ALL
        env:
        - name: AGENT_ROLE
          value: "analyst"
        - name: OPENAI_API_KEY
          valueFrom:
            secretKeyRef:
              name: qa-secrets
              key: openai-api-key
        envFrom:
        - configMapRef:
            name: qa-config
        ports:
        - containerPort: 8004
          name: health
        resources:
          requests:
            memory: "1Gi"
            cpu: "750m"
          limits:
            memory: "2Gi"
            cpu: "1500m"
        volumeMounts:
        - name: tmp
          mountPath: /tmp
        - name: logs
          mountPath: /app/logs
        livenessProbe:
          httpGet:
            path: /health
            port: 8004
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 8004
          initialDelaySeconds: 10
          periodSeconds: 5
        startupProbe:
          httpGet:
            path: /health
            port: 8004
          initialDelaySeconds: 10
          periodSeconds: 10
          failureThreshold: 30
          timeoutSeconds: 3
      volumes:
      - name: tmp
        emptyDir: {}
      - name: logs
        emptyDir: {}

# Security & Compliance Agent
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: security-compliance
  namespace: agentic-qa
  labels:
    app: security-compliance
    component: qa-agent
    role: security
spec:
  replicas: 1
  selector:
    matchLabels:
      app: security-compliance
  template:
    metadata:
      labels:
        app: security-compliance
        component: qa-agent
        role: security
    spec:
      securityContext:
        fsGroup: 1000
        runAsNonRoot: true
      containers:
      - name: security-compliance
        image: ghcr.io/maccracken/agnostic-security-compliance:latest
        imagePullPolicy: IfNotPresent
        securityContext:
          allowPrivilegeEscalation: false
          runAsUser: 1000
          readOnlyRootFilesystem: true
          capabilities:
            drop:
            - ALL
        env:
        - name: AGENT_ROLE
          value: "security-compliance"
        - name: OPENAI_API_KEY
          valueFrom:
            secretKeyRef:
              name: qa-secrets
              key: openai-api-key
        envFrom:
        - configMapRef:
            name: qa-config
        ports:
        - containerPort: 8005
          name: health
        resources:
          requests:
            memory: "1Gi"
            cpu: "750m"
          limits:
            memory: "2Gi"
            cpu: "1500m"
        volumeMounts:
        - name: tmp
          mountPath: /tmp
        - name: logs
          mountPath: /app/logs
        livenessProbe:
          httpGet:
            path: /health
            port: 8005
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 8005
          initialDelaySeconds: 10
          periodSeconds: 5
        startupProbe:
          httpGet:
            path: /health
            port: 8005
          initialDelaySeconds: 10
          periodSeconds: 10
          failureThreshold: 30
          timeoutSeconds: 3
      volumes:
      - name: tmp
        emptyDir: {}
      - name: logs
        emptyDir: {}

# Performance & Resilience Agent
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: performance-agent
  namespace: agentic-qa
  labels:
    app: performance-agent
    component: qa-agent
    role: performance
spec:
  replicas: 1
  selector:
    matchLabels:
      app: performance-agent
  template:
    metadata:
      labels:
        app: performance-agent
        component: qa-agent
        role: performance
    spec:
      securityContext:
        fsGroup: 1000
        runAsNonRoot: true
      containers:
      - name: performance-agent
        image: ghcr.io/maccracken/agnostic-performance:latest
        imagePullPolicy: IfNotPresent
        securityContext:
          allowPrivilegeEscalation: false
          runAsUser: 1000
          readOnlyRootFilesystem: true
          capabilities:
            drop:
            - ALL
        env:
        - name: AGENT_ROLE
          value: "performance"
        - name: OPENAI_API_KEY
          valueFrom:
            secretKeyRef:
              name: qa-secrets
              key: openai-api-key
        envFrom:
        - configMapRef:
            name: qa-config
        ports:
        - containerPort: 8006
          name: health
        resources:
          requests:
            memory: "1.5Gi"
            cpu: "1"
          limits:
            memory: "3Gi"
            cpu: "2"
        volumeMounts:
        - name: tmp
          mountPath: /tmp
        - name: logs
          mountPath: /app/logs
        livenessProbe:
          httpGet:
            path: /health
            port: 8006
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 8006
          initialDelaySeconds: 10
          periodSeconds: 5
        startupProbe:
          httpGet:
            path: /health
            port: 8006
          initialDelaySeconds: 10
          periodSeconds: 10
          failureThreshold: 30
          timeoutSeconds: 3
      volumes:
      - name: tmp
        emptyDir: {}
      - name: logs
        emptyDir: {}
